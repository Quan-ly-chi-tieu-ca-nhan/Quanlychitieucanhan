package com.example.personalexpensemanagementapplication.ui.theme

/**
 * Simple AppTheme enum to allow screens to select Light/Dark/System.
 * Kept minimal to match usage in SettingScreen.kt.
 */
enum class AppTheme {
    Light,
    Dark,
    System
}

